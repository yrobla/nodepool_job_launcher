============
Installation
============

At the command line::

    $ easy_install nodepool_job_launcher

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nodepool_job_launcher
    $ pip install nodepool_job_launcher